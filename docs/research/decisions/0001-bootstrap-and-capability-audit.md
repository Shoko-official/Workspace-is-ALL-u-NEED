# Decision 0001: Bootstrap and capability audit

Date: `2026-07-14`
Status: `ACCEPTED`
Issue: [#1](https://github.com/Shoko-official/Workspace-is-ALL-u-NEED/issues/1)

## Context

The remote repository was completely empty. The requested Sol, Terra, and Luna model variants cannot be selected or verified through the available runtime interface. GitHub administrator access is available. The supplied attachment contains a detailed research program but not the underlying report it references.

## Decision

1. Use the documented empty-repository exception for one minimal root commit on `main`, linked to issue #1.
2. Put all substantive governance work on `agent/1-bootstrap-research-governance` and deliver it through a PR.
3. Mark every exact role-model mapping as `NON CONTROLLABLE` and preserve only the functional division of work.
4. Treat the original report as `INPUT_MISSING`; do not infer or cite its contents.
5. Permit only governance, literature review, formal analysis, and protocol design until a later gate explicitly authorizes implementation.

## Consequences

The project can progress through M0 and M1 without pretending that model identity or missing inputs are known. Training and substantive implementation remain blocked. Independent review is functional rather than GitHub-identity-enforced until another reviewer identity is configured.
