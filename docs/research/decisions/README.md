# Decision records

Each decision record must include date, status, linked issue or milestone, context, decision, alternatives considered when material, evidence, and consequences. Accepted records are append-only; superseding decisions create a new record and link the record they replace.
